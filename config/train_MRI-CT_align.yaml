# CTAE and MRI_Encoder
in_channels: 1
out_channels: 1
img_size: 128
patch_size: 4
dim: 512
depth: 8
num_heads: 8
mlp_dim: 2048

# Training
margin: 1.0
epochs: 100
warm_epochs: 10
batch_size: 8
batch_print_interval: 50
checkpoint_save_interval: 3
save_path: 'Your path'  # 模型保存路径
device: 'cuda'
